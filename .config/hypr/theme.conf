# ┌────────────────────────────────────────────────────────────────────────────┐
# │                      COLOR PALETTE - CATPPUCCIN MOCHA                      │
# └────────────────────────────────────────────────────────────────────────────┘
# [INFO] This file defines the color variables for the entire Hyprland setup.

# ┌────────────────────────────────────────────────────────────────────────────┐
# │                               ACCENT COLORS                                │
# └────────────────────────────────────────────────────────────────────────────┘

$rosewater  = rgba(f5e0dcff)        # Rosewater
$flamingo   = rgba(f2cdcdff)        # Flamingo
$pink       = rgba(f5c2e7ff)        # Pink
$mauve      = rgba(cba6f7ff)        # Mauve
$red        = rgba(f38ba8ff)        # Red
$maroon     = rgba(eba0acff)        # Maroon
$peach      = rgba(fab387ff)        # Peach
$yellow     = rgba(f9e2afff)        # Yellow
$green      = rgba(a6e3a1ff)        # Green
$teal       = rgba(94e2d5ff)        # Teal
$sky        = rgba(89dcebff)        # Sky
$sapphire   = rgba(74c7ecff)        # Sapphire
$blue       = rgba(89b4faff)        # Blue
$lavender   = rgba(b4befeff)        # Lavender

# ┌────────────────────────────────────────────────────────────────────────────┐
# │                               NEUTRAL TONES                                │
# └────────────────────────────────────────────────────────────────────────────┘

# --- Text & Overlays ---
$text       = rgba(cdd6f4ff)        # Text
$subtext1   = rgba(bac2deff)        # Subtext 1
$subtext0   = rgba(a6adc8ff)        # Subtext 0
$overlay2   = rgba(9399b2ff)        # Overlay 2
$overlay1   = rgba(7f849cff)        # Overlay 1
$overlay0   = rgba(6c7086ff)        # Overlay 0

# --- Surfaces ---
$surface2   = rgba(585b70ff)        # Surface 2
$surface1   = rgba(45475aff)        # Surface 1
$surface0   = rgba(313244ff)        # Surface 0

# --- Base ---
$base       = rgba(1e1e2eff)        # Base
$mantle     = rgba(181825ff)        # Mantle
$crust      = rgba(11111bff)        # Crust
